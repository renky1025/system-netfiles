核心功能分解与数据模型

文件/文件夹管理
文件元数据表：file(id, parent_id, name, size, mime, md5, path, creator, create_time, update_time, status, version)
文件夹表：folder(id, parent_id, name, creator, create_time, update_time, status)
物理文件表：blob(id, bucket, object_key, store_type, size, checksum)
操作记录：file_op_log(id, user_id, file_id, op_type, op_time, client_ip)
权限管理
RBAC 基础表：user, role, user_role, permission, role_permission
ACL 细粒度：acl_entry(id, object_type, object_id, grantee, mask, inherit)
支持「继承+独立」模式：子目录可继承父目录权限，也可单独设置 
权限位示例：READ=1, WRITE=2, DELETE=4, SHARE=8, DOWNLOAD=16
合并计算：user_final_perm = role_perm ∪ acl_perm
分享管理
分享表：share(id, code, file_id, share_type, share_pwd, perm_mask, expire_time, creator, create_time, click_cnt, status)
外链访问日志：share_log(id, share_id, visitor_ip, access_time, action)
功能：有效期、提取码、下载次数上限、仅预览/可下载
用户权限管理
支持组织-部门-用户三级；批量导入/AD-LDAP 同步 
自助注册+后台审核+短信/邮件双因子
登录日志 & 异地提醒；密码策略 & 定期强制修改

关键流程设计
上传（含秒传/断点续传）
① 客户端计算 MD5+SHA1 → ② 前端询问 /file/exist → ③ 若存在直接绑定元数据（秒传）
④ 若不存在 → 获取 /file/chunk/params → ⑤ 分片上传 → ⑥ 合并 → ⑦ 异步落库+事件通知
权限判定（每次下载/预览/分享）
① 网关解析 Token 得 userId → ② 取角色权限 role_perm → ③ 取 ACL 权限 acl_perm → ④ 位运算合并 → ⑤ 若命中 READ 则放行并记录审计
分享安全
分享码长度 8 位，字典 0-9a-zA-Z（~218T 组合）
分享链接默认 7 天失效，可自定义；支持“仅公司内网 IP 可访问”
下载次数达到阈值自动失效
回收站 & 版本
删除→进入回收站（30 天自动清理）；物理文件引用计数=0 时异步删除
同名上传默认生成新版本，file.version 递增，可回滚


* 前端：Vue3 + TypeScript + Element-Plus
* 后端：Golang（Gin/Fiber）+ gRPC（可扩展微服务）
* 存储：PostgreSQL + MinIO / OSS / 本地存储
* 缓存：Redis
* 消息队列（可选）：Kafka / RabbitMQ（用于异步处理）



## 二、核心业务模块

### 1. 文件与文件夹管理

**核心数据结构**

| 表名            | 描述          |
| ------------- | ----------- |
| `file`        | 文件元信息（逻辑层）  |
| `folder`      | 文件夹信息       |
| `blob`        | 实体文件信息（物理层） |
| `file_op_log` | 文件操作日志      |

**主要功能**

* 文件上传（支持秒传 / 分片 / 断点续传）
* 文件版本管理（支持回滚）
* 回收站机制（软删除 + 定期清理）
* 批量操作（批量移动、删除、重命名）
* 操作日志留痕（包括来源 IP、时间、操作类型）

**上传流程**

1. 客户端计算 `MD5+SHA1`
2. 请求 `/file/exist`，判断是否已存在 → 秒传
3. 若不存在，获取 `/file/chunk/params` 分片参数
4. 分片上传至对象存储
5. 合并分片 → 生成 blob 记录
6. 异步写入数据库 → 通知事件队列（如更新索引、预生成预览）

---

### 2. 权限与访问控制（RBAC + ACL）

**设计原则**

* 基于角色（RBAC）+ 细粒度对象级 ACL
* 支持权限继承与独立覆盖
* 权限按位定义：
  `READ=1, WRITE=2, DELETE=4, SHARE=8, DOWNLOAD=16`

**权限计算公式**

```
final_perm = role_perm ∪ acl_perm
if (final_perm & READ) != 0 → 允许访问
```

**常见权限场景**

* 组织/部门管理员：可管理下级用户
* 文件夹继承父级权限（可手动关闭继承）
* 共享文件夹可配置“仅预览”或“可下载”

---

### 3. 分享与外链管理

| 表名          | 描述     |
| ----------- | ------ |
| `share`     | 分享记录   |
| `share_log` | 外链访问日志 |

**功能点**

* 生成带提取码（8位 0-9a-zA-Z）
* 可设有效期（默认 7 天）
* 支持访问来源限制（如内网 IP 白名单）
* 下载次数限制、访问审计、失效自动回收

---

### 4. 用户与组织管理

| 表名             | 描述      |
| -------------- | ------- |
| `user`         | 用户表     |
| `role`         | 角色表     |
| `user_role`    | 用户-角色映射 |
| `organization` | 组织/部门层级 |

**功能**

* 支持三层结构：组织 → 部门 → 用户
* 用户注册与审批（可接入 LDAP / AD 同步）
* 双因子认证（短信/邮件）
* 登录日志与异地登录提醒
* 密码策略（复杂度校验 + 定期修改）

---

### 5. 回收站与版本控制

**机制**

* 删除时进入回收站，保留 30 天
* 物理文件仅在引用计数为 0 时异步删除
* 文件多版本记录（`file.version` 递增）
* 用户可选择版本恢复

---


## 四、接口规划（示例）

| 模块 | 方法   | 接口路径           | 说明        |
| -- | ---- | -------------- | --------- |
| 文件 | POST | /file/upload   | 分片上传      |
| 文件 | GET  | /file/list     | 获取文件列表    |
| 文件 | POST | /file/move     | 批量移动      |
| 权限 | GET  | /perm/check    | 检查当前用户权限  |
| 分享 | POST | /share/create  | 创建分享链接    |
| 分享 | GET  | /share/{code}  | 访问分享文件    |
| 用户 | POST | /user/login    | 登录        |
| 用户 | POST | /user/register | 注册（含审批流程） |

---

## 五、难点与解决策略

| 难点        | 问题描述       | 可行解决方案                     |
| --------- | ---------- | -------------------------- |
| 分片上传一致性   | 多分片失败或重复提交 | Redis 记录分片状态 + 后端合并前校验     |
| 权限判定性能    | 每次访问都查多表   | 缓存用户最终权限 bitmask 到 Redis   |
| 大文件秒传安全   | 客户端伪造 MD5  | 校验 MD5+SHA1+文件头字节采样        |
| ACL 继承复杂性 | 层级深时判断慢    | 预计算权限路径哈希，写入权限缓存表          |
| 异步删除物理文件  | 文件被多个版本引用  | 引用计数 + 延迟删除队列（job-service） |
| 分享安全      | 链接泄露风险     | 支持提取码 + 访问次数限制 + IP 限制     |
| 审计合规性     | 文件访问无法追溯   | 全量操作日志 + 定期归档到冷存储          |

---

## 一、项目总体目标

构建一个支持 **多组织、多角色、多端访问** 的企业级网络文件管理平台，满足：

* 安全文件存储与共享
* 权限可控的协作机制
* 审计追踪与版本管理
* 管理员全局监控与干预能力

---

## 三、核心业务模块

### 1. 文件与文件夹管理

与原文一致，支持：

* 秒传 / 分片上传 / 断点续传
* 回收站（软删除 + 延迟清理）
* 版本管理（可回滚）
* 文件引用计数 + 异步物理删除

**新增管理后台能力：**

* 管理员可浏览所有用户文件
* 支持跨用户搜索（按文件名、MD5、上传者）
* 强制删除 / 恢复文件
* 文件占用统计（总空间、部门用量排行）

---

### 2. 权限系统（RBAC + ACL）

* 用户通过角色继承基础权限（RBAC）
* 细粒度 ACL 控制单文件或文件夹
* 支持权限继承（默认）与独立配置（覆盖父级）

**管理后台增强点：**

* 权限树形展示与编辑
* 一键重置用户权限
* 批量角色授权（部门级应用）
* 权限审计（查看谁访问了哪些文件）

---

### 3. 用户与组织管理

#### 功能概述

| 功能   | 用户端       | 管理端              |
| ---- | --------- | ---------------- |
| 用户注册 | ✅ 自助注册/邀请 | ✅ 审核审批/冻结账号      |
| 组织结构 | ✅ 查看部门层级  | ✅ 新增/删除部门、调整组织架构 |
| 角色分配 | ✅ 查看自身角色  | ✅ 分配角色/权限组       |
| 登录日志 | ✅ 个人历史    | ✅ 全局查询、导出、IP黑名单  |

#### 管理后台增强点

* 用户导入导出（Excel/CSV）
* LDAP / AD 同步配置界面
* 密码策略模板管理（复杂度、有效期）
* 异地登录提醒开关配置

---

### 4. 分享与外链管理

| 功能   | 用户端        | 管理端                  |
| ---- | ---------- | -------------------- |
| 分享创建 | ✅          | ✅（可代创建）              |
| 分享限制 | ✅（有效期、提取码） | ✅ 全局默认策略（例如 7 天失效）   |
| 分享日志 | ✅ 自查       | ✅ 全量审计（可按IP、时间、文件搜索） |

**新增功能：**

* 一键失效全公司分享链接
* 分享访问风险预警（异常访问次数/IP 地理异常）
* 分享访问统计看板（PV/UV/来源）

---

### 5. 回收站与版本控制

* 用户端可自行恢复文件
* 管理员可浏览所有回收站记录
* 文件超过保留期（默认30天）由后台任务清理
* 文件版本历史可按用户/文件追溯

**管理后台增强点：**

* 全局回收站查询
* 批量清理（按部门、时间、大小）
* 文件版本审计（可强制锁定最新版本）

---

### 6. 审计与日志系统

| 日志类型        | 内容             |
| ----------- | -------------- |
| file_op_log | 上传、修改、删除、下载等操作 |
| share_log   | 外链访问记录         |
| login_log   | 登录/登出/异常登录     |
| admin_log   | 管理后台操作记录（审计）   |

**管理后台增强点：**

* 日志可按条件筛选导出（用户、IP、时间段）
* 支持数据脱敏展示（邮箱/手机号）
* 操作行为回放（关键操作溯源）
* 审计报表（周/月度导出）

---

## 四、管理后台（Admin Portal）设计

### 技术栈

* **框架**：Vue3 + Vite + Element-Plus + Pinia
* **接口**：调用 `admin-service`
* **权限体系**：基于 JWT + 路由守卫 + 后端动态菜单

### 核心模块

| 模块   | 功能说明                    |
| ---- | ----------------------- |
| 用户管理 | 搜索/冻结/重置密码/导入导出         |
| 组织部门 | 树形结构编辑、层级管理             |
| 文件监管 | 全局文件浏览、强制删除、跨用户搜索       |
| 分享监管 | 分享记录列表、失效控制             |
| 权限管理 | 角色绑定、权限模板配置             |
| 系统设置 | 存储策略、分享策略、密码规则、操作日志导出   |
| 系统监控 | Redis命中率、文件总数、空间占用Top10 |
| 审计中心 | 日志检索、异常行为报警             |

---

## 五、接口设计示例（新增管理端）

| 方法   | 接口路径                 | 功能说明     |
| ---- | -------------------- | -------- |
| GET  | /admin/user/list     | 查询用户列表   |
| POST | /admin/user/freeze   | 冻结用户     |
| POST | /admin/file/delete   | 管理员删除文件  |
| GET  | /admin/log/audit     | 查询审计日志   |
| GET  | /admin/share/list    | 查询所有分享记录 |
| POST | /admin/share/disable | 批量失效分享链接 |
| GET  | /admin/stat/summary  | 获取系统总览统计 |

---

